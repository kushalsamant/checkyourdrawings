# Leave unset in local dev so Vite proxies /compare and /outputs to :8000.
# Set for production builds, e.g. VITE_API_BASE_URL=https://api.example.com
# VITE_API_BASE_URL=

# Pass 2 — production MVP (not required for local dev)
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
